const menuChoices = [
    'View all departments',
    'View all roles',
    'View all employees',
    // Add choices for other options
    'Exit',
  ];
  
  // Define inquirer prompts for each menu choice
  const menuPrompts = {
    // Prompt definitions
  };
  
  module.exports = {
    menuChoices,
    menuPrompts,
  };
  